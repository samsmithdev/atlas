export type AtlasSubjectSelectorItem = {
    id: string,
    shortcode: string;
    name: string;
}

export type AtlasProjectSelectorItem = {
    id: string;
    readableId: string;
    name: string;
}